---
description: Fetch a Notion page by ID
allowed-tools: [Bash]
---

Run `notion page $ARGUMENTS --json` and use the output as source context.

Summarize key details, owners, and action items from the page.
